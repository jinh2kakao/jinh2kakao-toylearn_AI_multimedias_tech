# VS Code에서 Vite로 React 프로젝트 설정하기

VS Code로 React 프로젝트를 설정하는 가장 일반적이고 효율적인 방법은, VS Code의 **통합 터미널**을 사용하여 `Vite` (최신 권장) 또는 `Create React App` (전통적인) 같은 도구로 **새로운 React 프로젝트를 생성**하는 것입니다.

수동으로 기존 프로젝트에 React를 추가하는 것은 웹팩(Webpack), 바벨(Babel) 등 복잡한 설정이 필요하므로, 특별한 경우가 아니라면 프로젝트 생성 도구를 사용하는 것이 좋습니다.

가장 빠르고 현대적인 방식인 **Vite**를 기준으로 자세하게 설명해 드립니다.

### 📍 시작 전 준비 사항

React 프로젝트를 시작하기 전에 컴퓨터에 두 가지가 반드시 설치되어 있어야 합니다.

1.  **VS Code:** (설치 완료)
2.  **Node.js:** [Node.js 공식 사이트](https://nodejs.org/)에서 **LTS 버전**을 다운로드하여 설치해 주세요. Node.js를 설치하면 `npm`(Node Package Manager)과 `npx`가 함께 설치됩니다.

---

### 🚀 1단계: Vite로 React 프로젝트 생성하기 (VS Code 터미널)

Vite는 매우 빠르고 가벼운 최신 프런트엔드 빌드 도구입니다.

1.  **VS Code 실행 및 폴더 열기**
    * 먼저 VS Code를 엽니다.
    * `파일(File)` > `폴더 열기(Open Folder...)`를 선택하여, 앞으로 **새 프로젝트를 생성할 상위 폴더**(예: `Documents/projects`)를 열어주세요.

2.  **VS Code 통합 터미널 열기**
    * VS Code 상단 메뉴에서 `터미널(Terminal)` > `새 터미널(New Terminal)`을 선택합니다. (단축키: `Ctrl` + `Shift` + `\``)
    * 이제 VS Code 하단에 터미널 창이 나타납니다.

3.  **Vite 생성 명령어 실행**
    * 터미널에 다음 명령어를 입력하고 Enter 키를 누릅니다.

    ```bash
    npm create vite@latest
    ```

4.  **프로젝트 설정 질문 답변하기**
    * 명령어를 실행하면 터미널에서 몇 가지 질문을 합니다. 키보드 화살표(↑, ↓)로 선택하고 Enter를 누르세요.
    * **`Project name:`**
        * 프로젝트 이름을 입력합니다. (예: `my-react-project`)
    * **`Select a framework:`**
        * **`React`**를 선택합니다.
    * **`Select a variant:`**
        * `JavaScript` 또는 `TypeScript` 중에 선택합니다. (React를 처음 시작한다면 **`JavaScript`**를 추천합니다.)

5.  **프로젝트 폴더로 이동 및 설치**
    * 프로젝트 생성이 완료되면, 터미널의 안내에 따라 다음 명령어들을 차례대로 입력합니다.

    ```bash
    # 1. 방금 생성한 프로젝트 폴더로 이동합니다.
    cd my-react-project

    # 2. 프로젝트에 필요한 패키지(React 포함)를 설치합니다.
    npm install
    ```

6.  **VS Code에서 새 프로젝트 폴더 열기 (중요!)**
    * 현재 VS Code는 *상위 폴더*를 열고 있는 상태입니다.
    * VS Code 상단 메뉴에서 `파일(File)` > `폴더 열기(Open Folder...)`를 다시 선택하여, 방금 생성된 **`my-react-project` 폴더를 직접 열어주세요.**
    * 이제 VS Code의 탐색기(왼쪽 사이드바)에 `package.json`, `src` 폴더 등이 보여야 합니다.

7.  **개발 서버 실행**
    * 새 프로젝트 폴더를 열었으면, 다시 VS Code 터미널(`Ctrl` + `Shift` + `\``)을 엽니다.
    * 다음 명령어를 입력하여 개발 서버를 시작합니다.

    ```bash
    npm run dev
    ```

    * 터미널에 `http://localhost:5173/` 같은 주소가 나타나면 `Ctrl` 키를 누른 채 클릭하여 브라우저에서 React 앱이 실행되는 것을 확인합니다.

---

### 🛠️ 2단계: React 개발을 위한 VS Code 필수 설정

React 프로젝트 생성을 완료했다면, 이제 VS Code 환경을 React 개발에 최적화할 차례입니다.

#### 1. 필수 확장 프로그램 설치

VS Code 왼쪽 사이드바에서 '확장(Extensions)' 아이콘(네모 블록 모양)을 클릭하고 다음 프로그램들을 검색하여 설치하세요.

* **ES7+ React/Redux/React-Native snippets**
    * **(필수)** `rfce`, `useState` 등 React에서 자주 쓰는 코드 조각들을 단축키로 자동 생성해 줍니다. (예: `rfce` 입력 후 Tab/Enter -> 기본 함수형 컴포넌트 완성)
* **Prettier - Code formatter**
    * **(강력 추천)** 코드를 정해진 규칙에 맞게 자동으로 정렬해 줍니다. (들여쓰기, 따옴표 통일 등)
* **ESLint**
    * **(강력 추천)** 코드의 문법 오류나 스타일 오류를 실시간으로 잡아줍니다.

#### 2. VS Code 설정 (settings.json)

코드를 저장할 때마다 Prettier가 자동으로 코드를 예쁘게 정렬하도록 설정하면 매우 편리합니다.

1.  VS Code에서 `Ctrl` + `Shift` + `P` (또는 `F1`) 키를 눌러 **명령 팔레트**를 엽니다.
2.  `settings.json`을 검색하여 `기본 설정: 사용자 설정 열기 (JSON)`를 선택합니다.
3.  열리는 `settings.json` 파일에 다음 코드를 추가합니다.

    ```json
    {
      // (기존 다른 설정들이 있다면 그대로 두고 아래 내용 추가)

      // 1. 코드를 저장할 때마다 자동으로 포맷팅(정렬) 실행
      "editor.formatOnSave": true,

      // 2. 기본 포맷터를 Prettier로 지정
      "editor.defaultFormatter": "esbenp.prettier-vscode",

      // 3. (선택 사항) Prettier가 JSX의 > 꺽쇠를 다음 줄로 내리지 않게 함
      "prettier.jsxBracketSameLine": true
    }
    ```

---

### 요약

1.  VS Code 터미널에서 `npm create vite@latest` 실행.
2.  `React`와 `JavaScript` 선택.
3.  `cd [프로젝트이름]`, `npm install` 실행.
4.  VS Code에서 `파일` > `폴더 열기`로 **방금 만든 프로젝트 폴더** 열기.
5.  `ES7+ Snippets`, `Prettier`, `ESLint` 확장 프로그램 설치.
6.  `settings.json`에 `formatOnSave` 설정 추가.
7.  VS Code 터미널에서 `npm run dev`로 개발 시작!